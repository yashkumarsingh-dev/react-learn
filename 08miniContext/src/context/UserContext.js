import React from 'react';

const UserContext = React.createContext();   // Create a context object: provides a way to pass data through the component tree without having to pass props down manually at every level.

export default UserContext;